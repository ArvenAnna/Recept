To start application you need:

1. Clone app
2. Run in terminal from project's root: docker-compose build, then: docker-compose up
3. Go inside postgres container, run :
   psql -Upostgres
   create database recept;
   \c recept
   \i /docker-entrypoint-initdb.d/recept_plain.sql
4. Run docker-compose down && docker-compose up
5. To make dump use inside postgres container
    pg_dump -U postgres recept > docker-entrypoint-initdb.d/recept_plain.sql


- обработка ошибок запросов - на рецепт с таким именем существует - show Error message
- search
- foto to ingredient (hierarchial structure?)
- add another style theme
- small file copies
- dropdown arrow not changed
- green background on side menu
- foto with file upload looks like a shit
- foto to be rotatable
- new fileupload component merged
- add form validation on empty fields (error tooltips)
- extract expandable blocks to separate component
- suggestion input - adding ingredient - doesn't retrieve new suggestions while typing symbols
	add border radius
